<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Jvnl" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag :app %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 text-gray-900">
    <div class="max-w-[1200px] mx-auto px-4 py-8">
      <header class="text-4xl font-black mb-8">
        Jasmijn Vink
      </header>

      <nav class="mb-8">
        <a href="<%= root_path %>" class="text-blue-600 hover:text-blue-800">home</a>
        <a href="<%= pictures_path %>" class="text-blue-600 hover:text-blue-800">alle beelden</a>
        <% if current_user %>
          <span class="mx-2">|</span>
          <a href="" class="text-blue-600 hover:text-blue-800">uploaden</a>
          <span class="mx-2">|</span>
          <%= button_to "uitloggen", destroy_user_session_path, method: :delete, class: "text-blue-600 hover:text-blue-800 border-none bg-transparent p-0" %>
        <% end %>
      </nav>

      <% if notice || alert %><section class="mb-8">
        <% if notice %><p class="bg-green-100 text-green-800 p-4 rounded"><%= notice %></p><% end %>
        <% if alert %><p class="bg-red-100 text-red-800 p-4 rounded"><%= alert %></p><% end %>
      </section><% end %>

      <main>
        <%= yield %>
      </main>
    </div>
  </body>
</html>
